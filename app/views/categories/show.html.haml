%ol.breadcrumb
  %li=link_to 'Home', root_path
  %li.active= @category.name

- @items.each do |item|
  = render 'items/item', :item => item, :feed => item.feed

.row
  .col-md-12
    .text-center
      = page_navigation_links @items