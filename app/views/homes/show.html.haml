- @feeds.each do |feed|
  - feed.items.limit(5).each do |item|
    %span=feed.name
    = link_to item.title, item.url
    %p!= item.description
    %br
    %p!= item.content
    %br
    -unless item.enclosure_url.nil?
      %img.thumbnail{:src => item.enclosure_url}
      %br
